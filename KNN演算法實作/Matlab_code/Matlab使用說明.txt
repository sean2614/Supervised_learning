版本 : Matlab - 2013a

程式預設 K = 10 ， 若要改成手動輸入 K 則刪除第四行的註解符號，並把第三行註解即可。

因為程式在計算執行時間時，會把手動輸入 K 所花的時間算進去，因此改為預設 K 值程式執行的秒數會比較少。

預設 K = 10的情況下，執行時間為3.5秒左右，扣除1.5秒多的資料讀取時間，實際計算時間為2秒，比C++語法，快15倍以上。

最後輸出結果在 output.csv檔上